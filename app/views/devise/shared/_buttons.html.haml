- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = button_to "Sign in with #{provider.to_s.titleize.split.first} Account", omniauth_authorize_path(resource_name, provider), class: "login-btn"
    %br

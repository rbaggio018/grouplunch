<%= form_for @transaction do |f| %>

  <%= f.fields_for :source, @transaction.source do |source| %>
    <%= source.label :email, 'From' %>
    <%= source.text_field :email %>
  <% end %>

  <%= f.label :amount %>
  <%= f.text_field :amount %>

  <%= f.submit 'Transfer' %>
<% end %>